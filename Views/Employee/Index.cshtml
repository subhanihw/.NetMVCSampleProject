@model List<SampleMVCApp.Models.EmployeeViewModel>


<h1>Hello</h1>
<ul>
    @foreach (var employee in Model) {
        <li>@employee.Name (@employee.PrimarySkill) (@employee.Experience)</li>
    }
</ul>

